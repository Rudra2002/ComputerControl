{
  "name": "computer-control-web",
  "private": true,
  "version": "1.0.0",
  "description": "ESP32 Computer Control Dashboard — Remote PC control via MQTT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "mqtt": "^5.10.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
